# Project Status

## Phase and objective

**Phase:** Scaffold complete

**Objective:** Publish the next independent, production-ready Kanban design; one catalog style remains (`kanban-skeuomorphic`).

## Completed

- Established the SvelteKit, Svelte 5, TypeScript, Tailwind CSS, Vite, Zod, Vitest, and Playwright scaffold.
- Implemented a build-time, file-backed catalog that validates required entry files and metadata, preserves each `DESIGN.md`, and lazy-loads previews.
- Added the project-local `ui-factory-design-loop` Pi skill to make the remaining style workflow repeatable across sessions: Herdr-visible concept/implementation/review/server topology, exactly ten concepts, only concept-selection and final-live-approval user gates, isolated worktrees, remediation until frontend approval, full verification, PR creation after approval, and post-merge cleanup.
- Established `published/` as the runtime publication boundary and `workbench/` as authoring-only: server catalog code imports only production-ready published metadata and handoffs, while client code imports only published preview modules.
- Added a test-only incomplete workbench fixture plus unit and post-build gates that fail if server or client discovery widens across the publication boundary.
- Made non-production entries under `published/` fail production builds; workbench, hidden, deprecated, and unknown slugs remain unavailable to public routes.
- Added the responsive semantic factory shell, a graceful empty-catalog state, published-design listing and detail routes, and isolated iframe preview route with reload recovery.
- Wired Tailwind through the factory route group while keeping preview documents outside its preflight and utilities.
- Covered schema, registry, and preview lookup behavior with 32 unit tests; Chromium tests cover the populated catalog listing, the published-design detail with its isolated preview, and unknown-design / unknown-preview 404 behavior, including document-style isolation.
- Published the first production-ready design entry, `kanban-glassmorphism`, so the catalog is no longer empty.
- Impeccable critique: the restrained hierarchy keeps attention on future previews and the publication message makes the empty state candid rather than promotional.
- Impeccable audit: semantic landmarks, visible focus, AA-oriented tokens, and bounded line lengths cover the shell; Chromium checks at 375px and 1280px found no horizontal overflow and preserved skip-link focus.
- Impeccable polish: removed starter UI and demo routes, used dividers instead of cards, and retained system type, a single cool accent, and concise product copy.
- Published `kanban-glassmorphism`, the first of the ten planned Kanban styles: a frosted translucent board over a soft low-chroma mist wash with backdrop blur, cool-gray hairlines, and a calm dusty-slate accent, authored production-ready. Its `fixtures.ts` **locks the shared Kanban content baseline** (4 columns: Backlog, In Progress, In Review [empty, for the empty state], Done; 9 cards; 5 members) that later styles re-skin. The self-contained `DESIGN.md` meets the product-vision handoff bar (color/typography/spacing/layout/responsive/motion/a11y, extension rules to new pages, do/don't, when-to-use/avoid/trade-offs, declared deps/assets/licenses, and an AI acceptance checklist). Cleared impeccable critique/audit/polish plus independent `pi.frontend-reviewer`/`pi.qa` passes; WCAG 2.2 AA verified by real-pixel contrast audit across every text role.
- Published `kanban-illustration`, the second of the ten planned Kanban styles and the first illustration direction: a warm parchment canvas with espresso ink, a disciplined mustard/teal/coral palette, hand-cut geometric shapes (starbursts, concentric circles, 2×3 square grip), rectangular cards with crisp offset shadows (`Npx Npx 0`, no blur), and serif headings/card titles over system-sans controls/meta. Walked the publication workflow end to end (workbench `draft` → `reviewed` → `production-ready` → moved to `published/`). `fixtures.ts` copied verbatim from the locked glass baseline; only the visual language changes. Illustration concentrated in composition (header starburst watermark + project-chip starburst) and state treatments (empty starburst, cut-paper error triangle, square grip, hard-edged skeleton). 2px espresso `--ink` outlines on major surfaces (header, columns, project chip, avatars, controls, error banner) and 1.5px `--rule-strong` on cards; no pure black/white, no colored side-stripes, no external fonts/assets/deps, no gradient text, no cited or imitated named artist. Self-contained `DESIGN.md` (18 sections plus the header) meets the product-vision handoff bar (color/typography/spacing/layout/responsive/motion/a11y, extension rules to new pages, do/don't, when-to-use/avoid/trade-offs, declared deps/assets/licenses, and an AI acceptance checklist). Added one focused Playwright smoke covering the detail page, the isolated preview header (`Sprint 24 · Board`), Backlog and In Review columns, the empty In Review state, the inline error banner + Retry button, the loading skeleton, and a filter `aria-pressed` interaction. Search/filter/view controls are visual-specimen-only (they toggle selection state, not business behavior), matching the glass baseline.
- Published `kanban-flat-material`, the third of the ten planned Kanban styles and the cleanest reference direction: a high-contrast Flat/Material board with a full-bleed deep-cobalt app bar, pale-blue canvas, solid near-white surfaces, bold cobalt primary actions (inverted to near-white on the bar), disciplined semantic stage dots (Backlog slate / In Progress blue / In Review amber / Done green), 12px radii, and a crisp layered 4dp/8dp-style navy elevation scale (1dp/2dp/4dp/8dp). Walked the publication workflow end to end (workbench `draft` → `reviewed` → `production-ready` → moved to `published/`). `fixtures.ts` copied verbatim from the locked glass baseline; only the visual language changes. OKLCH throughout, no pure black/white, no gradients, no backdrop blur, no glass/clay/neumorphic treatments, no gradient text, no colored side-stripes, no external fonts/assets/deps. Loading skeleton uses a gradient-free opacity pulse; error is a solid pale-red inline banner (no side-stripe); all header controls present; search/filter/view/drag/retry/dismiss are visual-specimen-only affordances (toggle selection state, not business behavior; drag is static), matching the glass baseline. Self-contained `DESIGN.md` (18 sections plus the header) meets the product-vision handoff bar.
- Frontend-review remediation of `kanban-flat-material`: added a contrasting cobalt `:focus-within` ring to the search container (the input keeps focus semantics, its redundant outline suppressed so keyboard focus is no longer an invisible near-white-on-near-white double ring); raised every filter chip and Board/List segmented button to a full 44px tap at all viewports; updated `DESIGN.md` to match (44px targets, search focus-within treatment, normal 0.16s transitions distinguished from the 1.4s reduced-motion-gated skeleton pulse, states explicitly marked visual/affordance demonstrations — `aria-pressed` toggles vs. search/Retry/dismiss affordances vs. static drag) and replaced the inaccurate "inherits the source repository's license" claim with the actual status (the repository declares no license — no `LICENSE` file, no `package.json` `license` field). The focused Playwright smoke asserts exactly: all five filter/view controls (Board, List, All, Mine, Due this week) are ≥44px tall at each of 375/768/1280; focusing the search field produces a container outline with ≥3:1 contrast against the field background (a meaningful visible-ring check, not merely a non-zero outline); and no horizontal document overflow in the preview at 375/768/1280.
- Published `kanban-neumorphism`, the fourth of the ten planned Kanban styles and the catalog's low-contrast-lifted-to-AA direction: a **Hairline-Ringed Neumorphism** board with a cool-gray monochrome base where raised cards, columns, and controls share the canvas color (`oklch(0.9 0.006 255)`) and depth comes only from paired near-light (upper-left) and darker (lower-right) extrusion shadows (tinted, no pure white/black). An explicit **1px darker inner hairline ring** (`inset 0 0 0 1px`, layered into the extrusion `box-shadow`) on every column, card, and selected/raised control keeps boundaries legible when relief is subtle. Selection is a complementary **pressed-in** well (`--press-sm` + stronger ring), not a colored fill. Walked the publication workflow end to end (workbench `draft` → `reviewed` → `production-ready` → moved to `published/`). `fixtures.ts` copied verbatim from the locked glass baseline; only the visual language changes. OKLCH throughout; AA-dark ink (`oklch(0.26 0.014 255)`, ≈7:1) and meta (`oklch(0.37 …)`, ≈5:1) on the cool-gray surface; 12px radii; no pure black/white, no gradients, no backdrop blur, no glass/clay/flat-material/illustration treatments, no colored side-stripes, no external fonts/assets/deps, no gradient text. Labels are deliberately monochrome pressed pills (meaning via text); whisper-chroma (chroma 0.04–0.13) appears only in tiny semantic cues (stage dots, priority, done, error banner), each always paired with a text label/icon so meaning never relies on the barely-there relief. All header controls present plus a visible loading skeleton (inset grooves + gradient-free opacity pulse), inline pale-clay error/retry/dismiss banner, empty In Review pressed well, static drag affordance, done and priority states; a cool-cobalt hover shadow highlight (`--hover-shadow`) swaps the cast/extrusion shadow of hovered task cards and raised secondary controls (inactive filter chips, inactive Board/List buttons, the more-actions icon, Add a card) for a clearly contrasting cobalt lower-right shadow while preserving the near-light upper-left shadow and the 1px hairline ring — no background changes anywhere (card/button/column/track faces all stay neutral; native CSS only); decorative only, never the sole state signal (the paired light/dark shadow, 1px hairline ring, focus ring, and pressed/selected semantics all remain); selected/pressed controls, the primary, and error actions are excluded so their semantic styles win. Search/New task/More actions/Add a card/retry/dismiss/drag remain visual-specimen-only (inert affordances demonstrating their treatments — no business behavior); only filters and Board/List toggle `aria-pressed`. Semantic Svelte 5, keyboard controls, contrasting dark search `:focus-within` ring (input kept as the semantic focus target) with its placeholder lifted to AA (`--ink-soft`, ≈5:1), a dark accent focus outline on the primary that reads against the surrounding app-bar surface at ≥3:1 (outline-offset seats the ring outside the dark fill — a near-white ring would vanish on the near-white surface), every relevant target ≥44×44px at 375/768/1280 (including the column more-actions and error-dismiss icon buttons — no sub-44px target remains), hover effects gated behind `(hover: hover)` (the decorative cool-cobalt hover shadow still applies instantly under reduced-motion; only the 0.16s fade and transform lifts are suppressed), reduced-motion behavior, no document overflow at those widths. Self-contained `DESIGN.md` (18 sections plus the header) meets the product-vision handoff bar (color/typography/spacing/layout/responsive/motion/a11y, extension rules to new pages, do/don't, when-to-use/avoid/trade-offs, declared deps/assets/licenses, and an AI acceptance checklist) and states the actual licensing status (repository declares no license). The focused Playwright smoke asserts exactly: the detail page + isolated preview render Sprint 24 · Board, Backlog, the empty In Review state, the inline error banner + Retry, the skeleton card, and the `aria-pressed` filter interaction; all five filter/view controls (≥44px tall) and every icon button — column more-actions and error dismiss (both ≥44×44px) — pass at each of 375/768/1280; focusing the search field produces a container outline with ≥3:1 contrast against the cool-gray field background; focusing the New task primary produces a dark accent outline with ≥3:1 contrast against the surrounding app-bar surface; hovering a representative card and the All filter chip leaves each face and its parent surface at the resting neutral background while the computed box-shadow changes to the documented cool-cobalt shadow (a cobalt color layer appears) and AA-dark text contrast stays ≥4.5:1; and no horizontal document overflow in the preview at 375/768/1280.

- Published `kanban-claymorphism`, the fifth of the ten planned Kanban styles, in the user-approved reference-faithful **Luminous Putty** direction: a deep indigo canvas (`oklch(0.22 0.055 272)`), darker app bar (`oklch(0.17 0.05 273)`), and medium-dark indigo cards and controls (`oklch(0.3 0.058 275)`) with light lavender text. Columns are transparent and open rather than raised filled slabs; compact 6–10px radii, tight card spacing, tiny dark labels with pastel dots, and short layered inset/cast shadows preserve the dense tactile reference. Selection combines an accent fill with the pressed shadow; hover keeps faces unchanged, deepens the shadow, and lifts cards 1px only when motion is allowed. The semantic Svelte 5 specimen retains the locked content and demonstrated empty, loading, error, drag, done, priority, selection, hover, focus, and reduced-motion states with no added dependencies, fonts, or assets. The self-contained `DESIGN.md` now copies the actual tokens, shadow recipes, component rules, extension guidance, AA/focus behavior, and no-license status. Focused Playwright coverage locks the dark visual signature, audits semantic text roles at ≥4.5:1 against their opaque parents, verifies the search and primary focus indicators, confirms 44px targets at 375/768/1280 while transparent borders preserve the approved 38px segmented and 36px icon faces, and checks reduced motion and horizontal overflow.

- Published `kanban-editorial`, the sixth of the ten planned Kanban styles and the **Editorial / Typographic** direction, faithful to the user-selected **Annual Report** concept: a near-white cool-paper canvas (`oklch(0.985 0.003 250)`), brighter paper card/control surfaces (`oklch(0.995 0.002 250)`), dark navy ink (`oklch(0.26 0.03 258)`), cool-gray hairlines (`oklch(0.84 0.006 255)`), and a restrained teal accent (`oklch(0.44 0.08 200)`) reserved for the 2px column rules, the done check, and the focus ring. A Georgia/Times system serif carries the masthead, column headings, and card titles; the system sans carries every control, label, date, count, and supporting datum. Composition preserves the concept: a sober serif report masthead, a compact uppercase sprint/board line, a thin dark navy 1px board rule, teal 2px rules under column headings, mostly-square bordered cards (3px radius) with **no decorative elevation, no gradients, no backdrop blur, no translucency**, crisp data-table rhythm, generous surrounding whitespace, and a formal two-colour print palette. Labels are tiny uppercase data keys with uniform navy dot separators; avatars are restrained uniform navy circles; done titles are struck through. Controls are understated outlined report controls; selected/primary state is a single dark navy fill. The concept's error side-stripe is overridden by the project's absolute side-stripe ban and translated to a full pale-red banner with a full `1px solid` hairline border plus icon/text. Walked the publication workflow end to end (workbench `draft` → `reviewed` → `production-ready` → moved to `published/`). `fixtures.ts` copied verbatim from the locked glass baseline; only the visual language changes. The self-contained `DESIGN.md` (18 sections plus header) meets the product-vision handoff bar (intent, canonical page, principles, exact OKLCH tokens, typography, spacing/sizing/radii/rules, layout, navigation, components/states, responsive, motion, accessibility, extension rules for settings/auth/forms/tables/detail pages, do/do-not, when-to-use/avoid/trade-offs, dependencies/assets/licenses with the no-license status, and an AI acceptance checklist). Focused Playwright smoke (test-first: confirmed RED on the missing entry before implementation) covers detail + isolated preview, the exact approved public summary, locked content, empty/error/loading states, an `aria-pressed` filter interaction, a full table-driven WCAG 2.2 AA contrast audit of every text role against its actual opaque parent surface (the audit climbs through intentionally-transparent editorial controls to the paper canvas), search and primary (navy-fill) focus rings at ≥3:1, all targets ≥44px at 375/768/1280, direct-preview exact-width overflow/responsive behavior, a reduced-motion static-skeleton check, and visual-signature assertions that prevent drift (cool paper/navy/teal palette, serif headings with sans data, teal column rules, bordered near-square cards, no box shadows/gradients/backdrop blur).
- Frontend-review remediation of `kanban-editorial`: (1) every segmented control (All/Mine/Due this week/Board/List) now gets a complete, unclipped ≥3:1 focus perimeter — the root cause (`.segmented { overflow: hidden }` clipping the offset outline) was removed and the ring renders on the compact face; (2) visible control chrome stays compact (~21–24px "face") matching the Annual Report reference while the semantic hit target remains ≥44px — the face is a separate element centred inside a transparent 44px button/label; (3) **zero rendered box-shadows** anywhere, including avatar overlap rings, which are now crisp 2px paper-coloured borders. Test-first: each regression assertion was confirmed RED for the expected reason (`.face` absent, `overflow: hidden`, avatar `box-shadow`) before the fix. Extended E2E locks all three (per-segmented-control Tab-walk focus + overflow-visible lock, compact-face-vs-target measure for search/filter/view/primary, and an all-element box-shadow scan), and the contrast audit now targets each control's face. `DESIGN.md` updated where implementation facts changed (controls, avatar, elevation, focus, do/don't, checklist).

- Published `kanban-swiss`, the seventh of the ten planned Kanban styles and the **Swiss / Minimal** direction, faithful to the user-approved artifact's **THEME 7 (Diagonal)** concept: a strict orthogonal grid of hairline rules and flat near-square cards set entirely in an austere system sans, on a warm near-white canvas (`oklch(0.97 0.004 90)`) with brighter warm paper surfaces (`oklch(0.99 0.003 90)`) and near-black warm ink (`oklch(0.22 0.008 90)`). A single **cobalt** accent — the exact approved `#1857c6` = `rgb(24,87,198)`, authored as `oklch(0.4886 0.1845 261.01)` (verified to render those exact pixels) and used consistently for the markers, primary fill, priority/done text, and focus ring (AA: ≈5.8:1 on paper; primary paper-on-cobalt ≈5.4:1). The 45-degree diagonal is reserved for **exactly two** showcased markers: a cobalt diamond on the **active column** (In Progress) and a cobalt diamond on **high-priority** cards. Selection is a full 2px cobalt border (never a side-stripe); priority High = cobalt diamond + cobalt word, Medium = square ink dot + ink word; the primary action is cobalt-filled; the error banner is a flat paper panel with a cobalt icon and full hairline border (no red anywhere — pure monochrome + one cobalt accent). All **five** members render as accessible team avatars in the header; the active column exposes its showcased state via `aria-label` ("active column"), and the selected card's computed accessible **name** includes "selected" — the selected article uses `aria-label="{title}, selected"` alone (no competing `aria-labelledby`, which would otherwise win and drop the state from the accessibility tree) while every other card keeps `aria-labelledby` for its title; none are interactive. Frontend-review remediation (second commit) corrected the first cut, which had invented a red header slash plus brand/selection/empty-state diamonds: the accent was restored to cobalt, the diagonal was trimmed back to the two approved markers, the five header avatars were added, clipped focus perimeters inside the desktop horizontal board scroller were fixed with internal breathing room (`0.4rem` scroller padding so the `overflow-x:auto`-computed vertical clip never cuts the column controls' offset rings at the top or at either scroll extreme), and the showcased states were exposed programmatically. `fixtures.ts` copied verbatim from the locked glass baseline; only the visual language changes. OKLCH throughout; no pure black/white, no gradients, no backdrop blur, no box shadows (avatar overlaps are crisp 2px borders), no side-stripes, no gradient text, no external fonts/assets/deps. All sans (no serif — clear of Editorial), no monospace (clear of Brutalism), no app-bar/elevation (clear of Flat Material). Compact-faced controls (≈22px face inside a transparent ≥44px target) preserve the austere Swiss scale while meeting WCAG 2.2 targets; loading skeleton uses a gradient-free opacity pulse. Strict test-first: the focused Playwright smoke was extended with the new assertions and run RED against `d4fa773` for the expected reasons (no team avatars; cobalt/limited-diagonal/aria/focus-clipping assertions not yet satisfied), then GREEN after remediation. The smoke asserts the approved THEME 7 signature (active-column + high-priority diamonds confirmed as non-identity 45° cobalt rotation matrices; diagonals limited to those two uses with no invented slash/brand/selection/empty diamonds; all-sans with no serif anywhere; no gradient/shadow/backdrop-blur; warm near-white canvas; cobalt priority/done/primary text ≥4.5:1 AA), all five named team avatars, the active-column `aria-label` and the selected card's computed accessible **name** (asserted via `getByRole('article', { name: /selected/i })`, which a competing `aria-labelledby` would have broken), the accent locked to exact `rgb(24,87,198)` rendered pixels, unclipped focus perimeters inside the scroller at 1280/768 (first and last column controls at both scroll extremes), and the responsive behaviours (≥44px targets and no horizontal document overflow at 375/768/1280), not implementation trivia. Self-contained `DESIGN.md` (18 sections plus header) meets the product-vision handoff bar.

- Published `kanban-brutalism`, the eighth of the ten planned Kanban styles and the **Brutalism** direction, faithful to the user-selected comparison artifact's **BLUEPRINT (technical schematic)** concept: a light compact brutalist board read as an engineering drafting sheet — off-white cool drafting-paper canvas (`oklch(0.94 0.005 250)`, the graph-paper field), brighter surface sheets for header/column-heads/cards/error (`oklch(0.975 0.003 250)`), near-black ink (`oklch(0.24 0.005 250)`), and **one deep technical-blue accent** `oklch(0.42 0.13 236)` (verified to render `rgb(0,84,137)`, ≈7.4:1 on cards). **Raw monospace throughout** (`ui-monospace, …`), a **foregrounded graph-paper grid** (an inline, dependency-free SVG data-URI tiling a 16×16 `--grid` stroke, so the drafting field is literally visible behind the opaque sheets), **harsh exposed 2px solid structural rules** (board frame, header/column/error separators, desktop column separators) and **1.5px card borders**, and **near-square cards** (radius 0). Coordinate/dimension notation is used purposefully: `✛` crosshair ticks prefix every card ID (accent), `N=` prefixes every column count (accent), A–D axis letters label columns, and `✛` registration crosshairs sit at the top-left/bottom-right board corners. Walked the publication workflow end to end (workbench `draft` → `reviewed` → `production-ready` → moved to `published/`); the `reviewed` gate ran `npm run check` (authoring gate, clean) and confirmed public invisibility — `/designs/kanban-brutalism` returns 404 "Design not found" and the entry is absent from the catalog listing — before promotion. `fixtures.ts` copied **verbatim** from the locked glass baseline; only the visual language changes. All CSS color tokens are OKLCH (the inline data-URI grid SVG serializes its stroke to the equivalent sRGB `#c1c4c8` = `rgb(193,196,200)`, verified pixel-equal to the `--grid` token, since a data-URI SVG cannot reference a CSS custom property); no pure black/white, no gradients, no gradient text, no `box-shadow` anywhere, no `backdrop-filter`, no colored side-stripes, no multi-hue tags (tags are monochrome ink-on-paper), no external fonts/assets/deps. The accent is reserved for meaningful marks only (active column name + head rule + `ACTIVE` tag, `✛` ticks, `N=` counts, HIGH priority, done check/due, selection, focus rings, Retry fill, error `!`/`SYNC PAUSED.`); the primary `New task` and project-chip are **ink**-filled, not accent. All canonical header/search/filter/view/team/New task controls plus empty, loading skeleton (reduced-motion-gated opacity blink), inline error with Retry/dismiss, static drag grip, done, priority, **selected** (full accent border + 3px outline + `aria-label="…, selected"`), hover (card border → accent, no shadow), keyboard focus, and reduced motion are demonstrated; search/New task/Retry/dismiss/drag are honest visual-specimen-only affordances and only filters + Board/List carry `aria-pressed`, matching the baseline. The **active column** (In Progress) exposes state via `aria-label` and the selected card's accessible **name** includes "selected" (aria-label alone, no competing `aria-labelledby`). Self-contained `DESIGN.md` (header + 18 sections) meets the product-vision handoff bar (intent, canonical page, principles, exact OKLCH tokens, typography, spacing/borders/radii/elevation, layout, navigation, components/states, responsive, motion, a11y, extension rules for settings/auth/forms/tables/detail, do/don't, use/avoid/trade-offs, no-license dependency status, AI acceptance checklist). Test-first: the focused Playwright smoke was confirmed **RED** for the expected missing-entry reason (404, heading not found) before implementation, then **GREEN**. Extended E2E locks: exact public summary; locked content/states; aria-pressed interaction; 5 accessible team avatars; active-column + selected-card programmatic state; the BLUEPRINT signature (graph-paper grid `url` background, all-monospace / no-sans / no-serif, off-white-not-pure-white canvas, near-black-not-pure-black ink, harsh solid >=1.5px card borders + >=2px frame, near-square cards, deep-blue accent channel dominance, `✛`/`N=` notation, **zero gradients / zero box-shadows / zero backdrop blur anywhere**); a table-driven WCAG 2.2 AA contrast audit over every semantic text role (climbing through transparency to the opaque parent); search focus ring >=3:1; reduced-motion skeleton suppression; and every control >=44×44 with no horizontal document overflow at 375/768/1280. Independent-frontend-review remediation (test-first, RED→GREEN for each behaviour): (1) icon targets are non-shrinking (`flex: none`) and the smoke now asserts **every** column more-actions plus dismiss at 375/768/1280 (In Progress more-actions previously rendered 37.44px wide); (2) the missing `reviewed` gate was actually performed (see workflow above); (3) desktop columns `flex: 1 0 15rem` grow to fill — no empty grid gutter — with tablet internal scrolling retained and a width assertion that failed at 240px before and passes after; (4) the instant card-hover border feedback was moved out of `prefers-reduced-motion: no-preference` so reduced-motion users retain hover (only transitions are motion-gated), with a reduced-motion hover assertion; (5) a Swiss-style regression now Tab-walks focus to the first/last column more-actions at both scroll extremes at 768 and measures unclipped perimeters (no clipping; the scroller carries internal padding); (6) docs made precise — primary is ink (removed from accent-role claims) and `--grid` is an OKLCH token whose inline SVG serialization to `#c1c4c8` is documented; (7) a compact visible `SEARCH` label was added (root DESIGN.md: inputs retain visible labels) while the accessible name `Search cards` is preserved (label-in-name). Live-review remediation: card hover was too subtle, so non-selected cards now get a clearly stronger multi-channel cue in the single accent hue — a pale technical-blue surface wash (`color-mix(in oklch, var(--accent) 16%, var(--surface))`) + a complete accent border perimeter + a crisp `translateY(-2px)` lift only when motion is allowed; on hover-capable devices, reduced motion keeps the immediate wash + perimeter while suppressing lift/transitions; the whole treatment is gated by `(hover: hover)`, so touch taps cannot leave a sticky state that resembles selection; hover is scoped to `:not(.is-selected)` so the selected card keeps its stronger treatment; no shadow/gradient/blur/second-hue/side-stripe, and cards stay non-interactive (no pointer/grab cursor). Test-first (RED on the missing wash and sticky touch state, then GREEN): computed-state polling replaces fixed timing sleeps, and the smoke asserts the wash + perimeter + motion lift, reduced-motion wash/perimeter with no transform, selected-state precedence, AA title contrast, no pointer/grab cursor, and unchanged card styling after a touch tap.

- Published `kanban-dark-neon`, the ninth of the ten planned Kanban styles and the **Dark / Neon** direction, faithful to the user-selected **HOLODECK WIREFRAME** concept: a dark, airy board on a near-black cyan-blue canvas (`oklch(0.13 0.035 230)`) where **transparent / near-transparent panels are defined by luminous cyan wireframe edges** — never by fills. Two type voices: **cool-white system-sans body/title/labels/controls** and **bright-cyan monospace metrics** (card IDs, counts, dates, checklist progress, header meta, status tags). A single cyan accent (`oklch(0.82 0.15 198)` edges/glow/metrics/dots/focus, mid `oklch(0.52 0.1 198)` resting edges) carries every meaningful mark; categorical label and avatar hues are intentionally unused (outlined monochrome). **Restrained semantic glow** — a `box-shadow` blur only, alpha ≤ ~0.35 — marks dots, the active column, selection, the primary action, and focus; **no gradients, no `backdrop-filter`, no drop/cast shadows, no multi-hue signage, no coloured side-stripes**. The app bar is a `rgba(cyan,5%)` wash with a cyan inset edge; selection/primary are transparent faces with bright cyan edges + modest glow; the error is a transparent red-tinted panel with a **full pale-red border** (`oklch(0.78 0.17 22)`). Walked the publication workflow end to end (workbench `draft` → `reviewed` → `production-ready` → moved to `published/`); the `reviewed` gate ran `npm run check` (clean) and confirmed public invisibility — `/designs/kanban-dark-neon` and its `/preview` return 404 "Design not found" and the entry is absent from the catalog listing — before promotion. `fixtures.ts` copied **verbatim** from the locked glass baseline; only the visual language changes. The inherent contrast trade-off of zero-fill wireframes is solved honestly: because panels are near-transparent, text reads on the near-black canvas, and the focused Playwright smoke audits WCAG 2.2 AA by **compositing each translucent panel layer over the canvas** (rather than measuring an un-composited tint that would overstate background luminance), so every semantic text role verifies ≥4.5:1 against its real effective backdrop. The self-contained `DESIGN.md` (18 sections plus header) meets the product-vision handoff bar. Test-first: the smoke was confirmed RED for the expected missing-entry reason (heading absent while in workbench) before implementation, then GREEN. It locks the dark visual signature (near-black canvas; cyan wireframe/near-transparent surfaces; cyan mono metrics; restrained glow; no backdrop blur/gradients/side-stripes), the showcased active-column/selected-card accessible states, five team avatars, and 44×44 targets + no horizontal overflow at 375/768/1280, with reduced motion suppressing the skeleton pulse and hover lift.

- Independent frontend-review remediation of `kanban-dark-neon` (separate commit): (1) the smoke's translucent-contrast audit now composites the **foreground RGBA and cumulative element/ancestor opacity** over the effective backdrop (not just the background layers), with a mutation self-check that injects `rgba(255,255,255,0.05)` text and proves it reads as ~1.1:1 (it previously false-passed at ~20:1); real opaque roles stay green. (2) The desktop/tablet board scroller's internal breathing room was raised from 4px to **6px** (the focus extent is 3px outline + 2px offset = 5px), and the smoke now keyboard-focuses the first/last column **Add-a-card** targets — the full-width, edge-flush controls whose perimeters actually exercise the clip — at both scroll extremes (768/1280) and measures unclipped perimeters, plus a direct ≥6px breathing-room lock on all four sides. (3) The responsive ≥44px table now also covers the clickable Search `<label>`, New task, Retry, and **every** "Add a card" target at 375/768/1280. (4) `DESIGN.md` corrected: the `--cyan` token is described factually (it drives glows **and** the active/hover cyan wash, the column separator, the card-foot hairline, and skeleton fills, not "only glows"), and a missing meaningful **"Empty, loading, and error states"** section was split out so the handoff has the required 18 `##` sections (matching the STATUS claim). The rejected workflow-history finding (no intermediate authoring commit) required no change. Test-first RED→GREEN for the two code fixes: F1 mutation 20.04→~1.1; F2 breathing room 4px→6px.

- Second frontend-review remediation of `kanban-dark-neon` (separate commit): (1) the smoke's contrast compositor no longer reparses alpha with a regex — `parseRgb` returns RGBA from the rendered canvas pixel (`d[3]/255`), so modern syntaxes the regex missed (`oklch(... / a)`, `rgb(... / a)`) are handled; `alphaOf` was removed. The mutation self-check was strengthened to a translucent **`oklch(1 0 0 / 0.4)`** foreground combined with **element AND ancestor opacity** (effective alpha `0.4 * 0.5 * 0.5 = 0.1`) and now asserts a bounded expected low contrast (`measured ≈ analytic expected`, with `expected` in `(1.0, 1.4)`) rather than merely `< 1.5`. RED→GREEN: old regex path diverged by **0.87** (treated the oklch text as opaque), canvas-alpha path matches expected (~1.1). (2) The responsive ≥44px table now asserts **both width and height** for the clickable Search label, New task, Retry, and every Add-a-card at 375/768/1280. (3) `DESIGN.md` corrected in two places where it still said `4px` scroller padding → now `6px` (matching the implementation and the 5px focus extent).

## Next

- Build the remaining Kanban style defined in `docs/catalog-roadmap.md` (`kanban-skeuomorphic`), through the documented publication workflow. It copies the locked baseline from `src/lib/designs/published/kanban-glassmorphism/fixtures.ts` and re-skins only the visuals.

## Decisions

- Catalog entries are repository files discovered at build time; no database, CMS, or generated registry.
- Public entries live under `src/lib/designs/published/`; unpublished authoring work lives under `src/lib/designs/workbench/` and is never imported by runtime code.
- Root `DESIGN.md` governs the factory UI; each entry's `DESIGN.md` is an independent portable handoff.
- Preview documents render in a separate route group without factory CSS so entry-owned styles remain isolated.
- Iframe sandboxing is deferred while previews are trusted repository content; require it before accepting untrusted or community-authored previews.
- Detail-page happy-path browser coverage is deferred until the first real published design provides a stable subject.
- Catalog direction is set in `docs/catalog-roadmap.md`: one page type (Kanban board) rendered in ten distinct visual styles (flat, glass, neumorphism, claymorphism, illustration, editorial, Swiss, brutalism, dark-neon, skeuomorphic), built one at a time. Style comparison on a fixed subject is the primary value.
- `kanban-glassmorphism` is the first published entry (dispatch priority) and therefore **locks the shared Kanban content baseline**: its `fixtures.ts` is canonical. Later styles copy it and change only the visual language. (Earlier advisory build order had `kanban-flat-material` locking the baseline; superseded by publication order.)
- Remaining catalog styles use `.pi/skills/ui-factory-design-loop/SKILL.md`; every worker and server must have a visible Herdr resource, and routine agent completion/review/remediation never creates an extra user checkpoint.

## Blockers

None.

## Last verified commands

- `npm run format`
- `npm run lint`
- `npm run check`
- `npm test`
- `npx playwright test tests/catalog.e2e.ts --grep "opens the kanban-dark-neon design"`
- `npm run test:e2e`
- `npm run build` (includes the client publication-boundary scan)
- `node scripts/check-client-publication-boundary.mjs`
- Mutation checks: widened client discovery fails the post-build scan; widened server preview discovery fails registry startup for missing metadata.
- `git diff --check main..HEAD`
- Skill RED/GREEN checks with fresh agents: baseline omitted the exact ten-concept/Herdr/two-gate contract; loading `ui-factory-design-loop` produced the required topology, pause policy, PR timing, and cleanup.
